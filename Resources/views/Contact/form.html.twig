{% extends '::default.html.twig' %}

{% block title %}Contact{% endblock %}

{% block body %}
  <form action="{{ path('de_baas_media_contact_form') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <input id="contact_submit" type="submit" />
  </form>
{% endblock %}
